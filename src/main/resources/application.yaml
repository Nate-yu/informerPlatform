server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/informer?serverTimezone=GMT%2b8
    username: root
    password: yb000719
#  redis:
#    host: 127.0.0.1
#    port: 6379

mybatis:
  mapper-locations: classpath:mapper/*.xml # 扫描所有mybatis的xml文件
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
